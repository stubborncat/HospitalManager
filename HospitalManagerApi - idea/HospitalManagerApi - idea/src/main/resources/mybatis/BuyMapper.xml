<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rabbiter.hospital.mapper.BuyMapper">
    <select id="updateByIdTest"  parameterType="com.rabbiter.hospital.pojo.Buy">
        UPDATE buy SET p_id=#{pId}, p_name=#{pName}, db_drug=#{dbDrug}, db_total_price=#{dbTotalPrice}, db_state=#{dbState}, dbend=#{dbend} WHERE (db_id = #{dbId})
    </select>


</mapper>